spring:
  application:
    name: ares
  cloud:
    gcp:
      sql:
        enabled: false
        database-name: "unused"
        instance-connection-name: "none:none"

ares:
  mcp:
    providers:
      notion:
        enabled: true
        server-url: "https://mcp.notion.com/mcp"
        auth-token: ${NOTION_MCP_TOKEN}
      github:
        enabled: false
        server-url: "https://api.githubcopilot.com/mcp"
        auth-token: ${GITHUB_MCP_TOKEN}
